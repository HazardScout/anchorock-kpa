export declare class ProcoreAuthModel {
    accessToken: string;
    refreshToken: string;
    clientId: string;
    clientSecret: string;
    constructor(accessToken: string, refreshToken: string);
}
